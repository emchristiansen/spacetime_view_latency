//! Focused tests for the Pilot ledger's line accounting. One test entity per file;
//! `capturing_writer` is the shared fixture.

mod capturing_writer;
mod each_record_is_one_line_carrying_its_sequence_and_kind;
