//! Tests for the range-validated repetition-block index. One test entity per file.

mod a_far_out_of_range_index_is_rejected;
mod the_count_itself_is_rejected;
mod the_first_index_is_accepted;
mod the_last_in_range_index_is_accepted;
