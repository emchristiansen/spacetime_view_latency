//! Graph-level tests folding the validation-owned complete-campaign fixture and asserting the
//! classifier's per-cell aggregation and gate over the real trusted graph.

mod a_uniform_campaign_classifies_every_cell_flat_equivalent;
